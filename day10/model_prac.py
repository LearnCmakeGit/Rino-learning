import torch
from torch import nn

class MLP(nn.Module):
    super():__init__()
    
    def __init__(self):
        slef.net = nn.sequential{
            nn.linear(in_features=1, 
            nn.Relu()
            nn.linear()
        }
        
    def forward(self,x):
        return self.net(x)